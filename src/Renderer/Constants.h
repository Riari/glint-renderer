#pragma once

namespace Renderer
{
    const float NEAR_PLANE = 0.1f;
    const float FAR_PLANE = 100.0f;
    const int SHADOW_MAP_RESOLUTION = 2048;
    const int TEXTURE_UNIT_DIRECTIONAL_SHADOWMAP = 5;
    const int TEXTURE_UNIT_OMNIDIRECTIONAL_SHADOWMAP = 6;
}
